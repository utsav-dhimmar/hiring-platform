# Backend URL (used by Vite proxy in vite.config.ts)
# To use direct backend calls, use: http://localhost:8000/api/v1
# To use Vite proxy, use: /api/v1
VITE_API_URL=/api/v1

VITE_RESUME_MAX_SIZE_MB=5
